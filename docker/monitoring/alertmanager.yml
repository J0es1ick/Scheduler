route:
  receiver: operations
  group_by: [alertname, job, kind]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
receivers:
  - name: operations
    webhook_configs:
      - url_file: /run/secrets/alerts-webhook-url
        send_resolved: true
